"""Markable — assessment preparation, AI-marking, and curriculum-intelligence
pipeline for paper-based school assessments.

See PROJECT_BRIEF.md for the founding spec and CLAUDE.md for locked-in
architecture decisions. Only Phase 1 (`ingest` + `build`) is implemented;
`scan`, `mark`, `report`, `curriculum`, and `tag` are stubs.
"""

__version__ = "0.1.0"
